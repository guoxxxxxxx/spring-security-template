spring:
  application:
    name: spring-security-template
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/template_spring_security_database?serverTimezone=UTC
    type: com.alibaba.druid.pool.DruidDataSource
  jpa:
    hibernate:
      ddl-auto: update
  data:
    redis:
      host: 152.136.216.12
      database: 0

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0


server:
  port: 8888

#  servlet:
#    context-path: /api/fileManager
